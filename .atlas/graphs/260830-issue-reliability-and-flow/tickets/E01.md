<!-- atlas:auto -->
# E01 · Lock remoto risolve il nome del remote nel repository del progetto

> Ramo: #24 Lock remoto · Tipo: task · Modo: AFK
> Bloccato da: nessuno, prendibile subito
> Stato e dipendenze vivono in `../graph.json`, e si cambiano solo con uno script di mutazione.

## Domanda

Risolvi lock.remote dal nome configurato, per esempio origin, all'URL Git prima di creare il trasporto nel repository di servizio. Distingui configurazione non risolvibile da errore di rete, aggiorna il contratto e aggiungi test sia per nome sia per URL.
<!-- /atlas:auto -->

## Lavorazione

<!-- appunti, alternative scartate, link agli artefatti prodotti -->

## Risposta

<!-- da compilare alla chiusura; la sintesi finisce da sola in map.md.

### Scelte non canoniche
Cosa hai deciso a tavolino, non dettato dal documento di design. Lascia vuoto se non ce ne sono.

### Debito dichiarato
Cosa lasci volutamente incompleto, e perché. Lascia vuoto se non ce n'è.

### Autorizzazioni ricevute
Se hai agito oltre l'ambito originale del nodo su indicazione esplicita dell'umano, registra qui cosa e quando. -->

### Sintesi

Risolto `lock.remote` dal nome del remote all'URL Git nel checkout del progetto prima di creare il trasporto; gli URL configurati passano direttamente. Un nome non risolvibile viene distinto dall'errore di rete del trasporto, con contratto e test aggiornati.

### Verifiche

- Test mirati: `python3 -m unittest tests.test_platform.Dispatch`, 8/8 passati.
- Suite completa: 399 test, 2 failure e 4 errori preesistenti su reclaim/liveness/concorrenza.
- Sintassi verificata con `python3 -m py_compile atlascli/dispatch.py tests/test_platform.py`.

### Scelte non canoniche

Nessuna.

### Debito dichiarato

Nessuno introdotto da E01.

### Autorizzazioni ricevute

Nessuna.
