<!-- atlas:auto -->
# G05 · Grafite pronta da pubblicare

> Ramo: Grafite a monte · Tipo: task · Modo: AFK
> Bloccato da: G01, G02
> Stato e dipendenze vivono in `../graph.json`, e si cambiano solo con uno script di mutazione.

## Domanda

Grafite e' una repo a se': Atlas la consuma riscaricandola, non copiandone i sorgenti a mano. Porta a casa il giro dentro la repo: bump di versione in package.json (oggi 1.1.0), README che spiega quando si usa la variante offline e quando quella con l'@import, commit con messaggio in italiano.

Il push non si fa. E' una repo pubblica su GitHub e nessun push parte senza un ok esplicito, che qui non c'e' perche' questo nodo lavora senza nessuno davanti. Ferma il lavoro al commit locale e scrivi nella risposta che resta da pushare.

E' fatto quando: la repo e' committata, la versione e' salita, il README dice a un consumatore offline quale foglio prendere, e il push resta da fare.
<!-- /atlas:auto -->

## Lavorazione

<!-- appunti, alternative scartate, link agli artefatti prodotti -->

## Risposta

**Versione:** bump a 1.2.0 (minor release). Le ricette dense di G03 e le ricette canvas di G04 sono additive e backward-compatible; semver prevede minor per feature nuove senza breaking change (major sarebbe per rompa API, patch per bugfix). Non è una major perché nessuna classe esistente è stata toccata, solo aggiunte 132 righe di ricette nuove.

**Coerenza verificata.**
- dist/grafite.offline.css rigenerato: contiene tutte le ricette di G03 (dense) e G04 (canvas), no @import remoti.
- dist/grafite.fonts.inline.css rigenerato: 203 KB con 8 tagli font inline come data: URI, zero import esterni.
- README.md completo: sezioni offline e font inline spiegano al consumatore offline quale foglio importare e perché (G02/G01). Nessun contrasto fra le tre varianti (con @import, senza font, con font inline).
- docs/grafite.md sincronizzato: documenta superfici dense (G03) e canvas di grafo (G04) col perché di ogni scelta, allineato ai token e alla filosofia.
- build-offline.mjs e build-fonts-inline.mjs funzionanti: rigenera i CSS al bisogno, idempotente.
- Nessuna dipendenza nuova in package.json.

**Esclusioni corrette:** docs/screenshots/ (artefatto di verifica) non incluso nel commit. Candidato opzionale se un consumatore vuole vedere i font inline "provati", ma non è asset di default.

**Commit:** `08cd760` · messaggio Conventional Commits in italiano (feat(css): superfici dense...). Corpo spiega il cosa e il perché per ogni nodo (G03/G04/G02/G01).

**Push:** resta da fare. La repo è pubblica e il push è manuale, no automazione. Chi legge la Risposta sa che il lavoro è committato e pronto, il push è il passo seguente dopo un ok esplicito.

### Scelte non canoniche
Minor invece di major: Grafite cresce per ricette, non per rotture. Additive = minor.

### Debito dichiarato
—

### Autorizzazioni ricevute
—
