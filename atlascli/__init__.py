"""Il CLI globale di Atlas: installa/aggiorna l'harness nei progetti, aggiorna se stesso.

Distinto da payload/, che e' il motore che finisce dentro un progetto ospite: qui
la rete e' consentita (solo stdlib, verso GitHub), li' resta vietata per contratto.
"""
